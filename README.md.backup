# Servidor

Todos os dados ficam salvos no servidor por segurança, assim a cada interação o frontend recebe uma cópia para não haver trapaças, a comunicação ocorre por socket ultilizando a biblioteca socket.io, o bot foi desenvolvido pelo método de minimax.